/** @type {import('next').NextConfig} */
const nextConfig = {
  //*output: 'export',
  reactStrictMode: true,
  basePath: '',
  assetPrefix: '',
  images: {
    unoptimized: true
  }
};

export default nextConfig;
